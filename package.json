{
  "name": "durudex-web",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "pnpm -C packages/app dev",
    "build": "pnpm -C packages/app build",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "vitest": "^0.18.1"
  }
}
